[package]
name = "templateer"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["cdylib"]

[dependencies]
egui = "0.22.0"
eframe = "0.22.0"
miette = "5.10"
reqwest = "0.11"
flexver-rs = "0.1.2"
futures = "0.3"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
# RFD doesn't currently support save dialogs on wasm
rfd = "0.11.4"
tokio = { version = "1", features = ["full"] }
xml_dom = "0.2.6"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2.87"
wasm-bindgen-futures = "0.4.37"
js-sys = "0.3.64"

[target.'cfg(target_arch = "wasm32")'.dependencies.web-sys]
version = "0.3.64"
features = [
    "Document",
    "DomParser",
    "Element",
    "HtmlCollection",
    "Node",
    "NodeList",
    "SupportedType",
    "console",
]
